namespace Arch_lab.Models.Enum
{
    public enum StatusCode
    {
        UserNotExists = 1,
        UserNotFound = 2,
        InvalidSignature = 3,
        OK = 200,
        InternalServerError = 500
    }
}
